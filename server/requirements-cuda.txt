# Windows + WSL2 (Ubuntu) + NVIDIA GPU (Ampere 以降推奨) で動かす想定
# python 3.11 以降推奨
#
# CUDA 対応 torch は別 index から先に入れること (setup.md 参照):
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch torchaudio

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic-settings==2.5.2

# STT — faster-whisper は ctranslate2 が CUDA libs を引っ張る
faster-whisper==1.0.3

# LLM (Ollama HTTP 経由)
httpx==0.27.2

# TTS — Irodori-TTS-Lite (改造済み fork)。
# irodori_tts 親パッケージ・pyopenjtalk・infer モジュールを同梱済み。
# setup.md の手順 1-2 ステップ(2) で先に pip install しておくこと。
# torchaudio はリサンプル用にここで明示
torchaudio>=2.1
numpy>=1.26

# 開発支援
python-dotenv==1.0.1
