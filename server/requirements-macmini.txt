# Mac mini など非 CUDA 環境向け (TTS_BACKEND=voicevox)
# docs/setup-macmini.md 参照
# python 3.11 以降推奨

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic-settings==2.5.2

# STT — faster-whisper は Apple Silicon (Metal / CoreML) でも動く
faster-whisper==1.0.3

# LLM (Ollama HTTP) と TTS (VOICEVOX HTTP) 両方とも httpx で叩く
httpx==0.27.2

# 定期発話スケジューラ (cron 式の評価)
croniter>=2.0

# 開発支援
python-dotenv==1.0.1
